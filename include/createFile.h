#ifndef CREATEFILE_H
#define CREATEFILE_H

// Function to create a file and write a string to it
void createfile();
int createfileNonInteractive(const char *fileName, const char *content);

#endif // CREATEFILE_H
